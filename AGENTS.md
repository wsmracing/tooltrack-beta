# ToolTrack project notes

- Mobile-first Next.js App Router application.
- Use the shared design tokens in `app/globals.css`; do not introduce page-specific font scales or colour systems.
- Receipts and invoices are private. Never expose document paths through public lookup responses.
- Public lookup responses must contain only non-identifying asset information.
